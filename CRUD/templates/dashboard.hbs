<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - {{user.name}}</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #0f1419 0%, #1a2332 100%);
            color: #fff;
            min-height: 100vh;
        }

        /* Navbar */
        .navbar {
            background: rgba(15, 20, 25, 0.9);
            border-bottom: 2px solid #00d4ff;
            padding: 1rem 0;
            position: sticky;
            top: 0;
            z-index: 100;
            backdrop-filter: blur(10px);
        }

        .nav-container {
            max-width: 1200px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 2rem;
        }

        .logo {
            font-size: 1.8rem;
            font-weight: bold;
            color: #00d4ff;
            text-decoration: none;
            display: flex;
            align-items: center;
        }

        .nav-menu {
            display: flex;
            list-style: none;
            gap: 2rem;
            align-items: center;
        }

        .nav-link {
            color: #ccc;
            text-decoration: none;
            font-weight: 500;
            transition: 0.3s;
        }

        .nav-link:hover {
            color: #00d4ff;
        }

        .nav-link.active {
            color: #00d4ff;
            border-bottom: 2px solid #00d4ff;
            padding-bottom: 0.25rem;
        }

        .user-info {
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .user-avatar {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            border: 2px solid #00d4ff;
            object-fit: cover;
        }

        .logout-btn {
            background: #ff4444;
            color: white;
            border: none;
            padding: 0.5rem 1rem;
            border-radius: 5px;
            cursor: pointer;
            transition: 0.3s;
            text-decoration: none;
            display: inline-block;
        }

        .logout-btn:hover {
            background: #cc0000;
        }

        /* Main Container */
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem;
        }

        /* Profile Header */
        .profile-header {
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid #00d4ff;
            border-radius: 15px;
            padding: 2rem;
            margin-bottom: 2rem;
            display: flex;
            gap: 2rem;
            align-items: center;
        }

        .profile-avatar {
            width: 120px;
            height: 120px;
            border-radius: 10px;
            border: 3px solid #00d4ff;
            object-fit: cover;
        }

        .profile-info h1 {
            font-size: 2rem;
            margin-bottom: 0.5rem;
            color: #00d4ff;
        }

        .profile-title {
            color: #0066ff;
            font-size: 1.1rem;
            margin-bottom: 1rem;
        }

        .profile-bio {
            color: #ccc;
            margin-bottom: 1rem;
        }

        .profile-stats {
            display: flex;
            gap: 2rem;
        }

        .stat {
            text-align: center;
        }

        .stat-number {
            font-size: 1.5rem;
            color: #00d4ff;
            font-weight: bold;
        }

        .stat-label {
            color: #888;
            font-size: 0.9rem;
        }

        .edit-profile-link {
            background: #0066ff;
            color: white;
            padding: 0.75rem 1.5rem;
            border-radius: 5px;
            text-decoration: none;
            transition: 0.3s;
            display: inline-block;
            margin-top: 1rem;
        }

        .edit-profile-link:hover {
            background: #0052cc;
        }

        /* Projects Section */
        .section-title {
            font-size: 1.8rem;
            margin-bottom: 1.5rem;
            color: #00d4ff;
            border-bottom: 2px solid #0066ff;
            padding-bottom: 1rem;
        }

        .projects-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 2rem;
            margin-bottom: 2rem;
        }

        .project-card {
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid #00d4ff;
            border-radius: 10px;
            padding: 1.5rem;
            transition: 0.3s;
        }

        .project-card:hover {
            transform: translateY(-5px);
            border-color: #0066ff;
            box-shadow: 0 10px 30px rgba(0, 212, 255, 0.2);
        }

        .project-title {
            font-size: 1.3rem;
            color: #00d4ff;
            margin-bottom: 0.5rem;
        }

        .project-desc {
            color: #ccc;
            margin-bottom: 1rem;
            font-size: 0.95rem;
        }

        .project-tech {
            background: rgba(0, 102, 255, 0.1);
            color: #0066ff;
            padding: 0.25rem 0.75rem;
            border-radius: 15px;
            font-size: 0.85rem;
            display: inline-block;
            margin-bottom: 1rem;
        }

        .project-actions {
            display: flex;
            gap: 0.5rem;
        }

        .btn-small {
            padding: 0.5rem 1rem;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 0.9rem;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 0.3rem;
            transition: 0.3s;
        }

        .btn-view {
            background: #0066ff;
            color: white;
        }

        .btn-view:hover {
            background: #0052cc;
        }

        .btn-delete {
            background: #ff4444;
            color: white;
        }

        .btn-delete:hover {
            background: #cc0000;
        }

        .empty-state {
            text-align: center;
            padding: 3rem;
            color: #888;
        }

        .empty-state i {
            font-size: 3rem;
            color: #00d4ff;
            margin-bottom: 1rem;
        }

        .add-project-btn {
            background: #0066ff;
            color: white;
            padding: 1rem 2rem;
            border-radius: 5px;
            text-decoration: none;
            display: inline-block;
            margin-bottom: 2rem;
            transition: 0.3s;
        }

        .add-project-btn:hover {
            background: #0052cc;
        }

        /* Alert Messages */
        .alert {
            padding: 1rem;
            border-radius: 5px;
            margin-bottom: 1rem;
        }

        .alert-success {
            background: rgba(76, 175, 80, 0.2);
            color: #4caf50;
            border: 1px solid #4caf50;
        }

        .alert-error {
            background: rgba(244, 67, 54, 0.2);
            color: #f44336;
            border: 1px solid #f44336;
        }

        /* Footer */
        .footer {
            background: rgba(15, 20, 25, 0.9);
            border-top: 2px solid #00d4ff;
            padding: 2rem;
            text-align: center;
            color: #888;
            margin-top: 3rem;
        }

        @media (max-width: 768px) {
            .profile-header {
                flex-direction: column;
                text-align: center;
            }

            .projects-grid {
                grid-template-columns: 1fr;
            }

            .nav-menu {
                gap: 1rem;
            }
        }
    </style>
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar">
        <div class="nav-container">
            <a href="/" class="logo"><i class="fas fa-laptop-code"></i> Portfolio</a>
            <ul class="nav-menu">
                <li><a href="/" class="nav-link">Home</a></li>
                <li><a href="/projects" class="nav-link">All Projects</a></li>
                <li><a href="/dashboard" class="nav-link active">Dashboard</a></li>
                <li><a href="/contact" class="nav-link">Contact</a></li>
                <li class="user-info">
                    <img src="{{user.avatar}}" alt="Profile" class="user-avatar">
                    <span>{{user.name}}</span>
                    <a href="/logout" class="logout-btn">Logout</a>
                </li>
            </ul>
        </div>
    </nav>

    <!-- Main Content -->
    <div class="container">
        {{#if message}}
        <div class="alert alert-success">
            {{message}}
        </div>
        {{/if}}

        {{#if error}}
        <div class="alert alert-error">
            {{error}}
        </div>
        {{/if}}

        <!-- Profile Header -->
        <div class="profile-header">
            <img src="{{user.avatar}}" alt="{{user.name}}" class="profile-avatar">
            <div class="profile-info">
                <h1>{{user.name}}</h1>
                <p class="profile-title">{{user.title}}</p>
                {{#if user.bio}}
                <p class="profile-bio">{{user.bio}}</p>
                {{/if}}
                {{#if user.location}}
                <p class="profile-bio"><i class="fas fa-map-marker-alt"></i> {{user.location}}</p>
                {{/if}}
                <div class="profile-stats">
                    <div class="stat">
                        <div class="stat-number">{{projects.length}}</div>
                        <div class="stat-label">Projects</div>
                    </div>
                    <div class="stat">
                        <div class="stat-number">{{#if user.skills}}{{user.skills}}{{else}}5+{{/if}}</div>
                        <div class="stat-label">Skills</div>
                    </div>
                </div>
                <a href="/profile/{{user._id}}" class="edit-profile-link"><i class="fas fa-edit"></i> Edit Profile</a>
            </div>
        </div>

        <!-- Projects Section -->
        <div style="margin-top: 3rem;">
            <h2 class="section-title"><i class="fas fa-projects"></i> My Projects</h2>
            
            <a href="/add-project" class="add-project-btn"><i class="fas fa-plus"></i> Add New Project</a>

            {{#if projects}}
            <div class="projects-grid">
                {{#each projects}}
                <div class="project-card">
                    <h3 class="project-title">{{this.title}}</h3>
                    <p class="project-desc">{{this.description}}</p>
                    <span class="project-tech">{{this.technologies}}</span>
                    <div class="project-actions">
                        <a href="/project/{{this._id}}" class="btn-small btn-view">
                            <i class="fas fa-eye"></i> View
                        </a>
                        <form method="POST" action="/delete-project/{{this._id}}" style="display:inline;">
                            <button type="submit" class="btn-small btn-delete" onclick="return confirm('Are you sure?');">
                                <i class="fas fa-trash"></i> Delete
                            </button>
                        </form>
                    </div>
                </div>
                {{/each}}
            </div>
            {{else}}
            <div class="empty-state">
                <i class="fas fa-inbox"></i>
                <p>No projects yet. <a href="/add-project" style="color: #00d4ff;">Create your first project!</a></p>
            </div>
            {{/if}}
        </div>
    </div>

    <!-- Footer -->
    <footer class="footer">
        <p>&copy; 2024 {{user.name}}. All rights reserved.</p>
    </footer>
</body>
</html>
