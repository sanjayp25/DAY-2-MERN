<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - {{user.name}}'s Portfolio</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #0f1419 0%, #1a1a2e 100%);
            color: #fff;
            min-height: 100vh;
        }

        @keyframes slideIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        /* Navbar */
        .navbar {
            background: rgba(15, 20, 25, 0.95);
            border-bottom: 2px solid #667eea;
            padding: 1rem 0;
            position: sticky;
            top: 0;
            z-index: 100;
            backdrop-filter: blur(10px);
            animation: slideIn 0.5s ease;
        }

        .nav-container {
            max-width: 1200px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 2rem;
        }

        .logo {
            font-size: 1.8rem;
            font-weight: bold;
            background: linear-gradient(135deg, #667eea, #764ba2);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            text-decoration: none;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .nav-menu {
            display: flex;
            list-style: none;
            gap: 2rem;
            align-items: center;
            flex: 1;
            justify-content: center;
        }

        .nav-link {
            color: #b0b9c1;
            text-decoration: none;
            font-weight: 500;
            transition: all 0.3s ease;
            position: relative;
        }

        .nav-link:hover {
            color: #667eea;
        }

        .nav-link.active {
            color: #667eea;
        }

        .nav-link::after {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 0;
            width: 0;
            height: 2px;
            background: linear-gradient(90deg, #667eea, #764ba2);
            transition: width 0.3s ease;
        }

        .nav-link:hover::after,
        .nav-link.active::after {
            width: 100%;
        }

        .user-info {
            display: flex;
            align-items: center;
            gap: 1.5rem;
        }

        .user-avatar {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            border: 2px solid #667eea;
            object-fit: cover;
        }

        .logout-btn {
            background: linear-gradient(135deg, #ff6b6b, #ee5a52);
            color: white;
            border: none;
            padding: 0.6rem 1.2rem;
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-block;
            font-weight: 600;
            font-size: 0.9rem;
        }

        .logout-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(255, 107, 107, 0.3);
        }

        /* Main Container */
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem;
        }

        /* Profile Header */
        .profile-header {
            background: linear-gradient(135deg, rgba(102, 126, 234, 0.1) 0%, rgba(118, 75, 162, 0.1) 100%);
            border: 2px solid #667eea;
            border-radius: 20px;
            padding: 3rem;
            margin-bottom: 3rem;
            display: flex;
            gap: 2rem;
            align-items: center;
            animation: slideIn 0.6s ease;
        }

        .profile-avatar {
            width: 150px;
            height: 150px;
            border-radius: 20px;
            border: 3px solid #667eea;
            object-fit: cover;
            box-shadow: 0 15px 40px rgba(102, 126, 234, 0.3);
        }

        .profile-info h1 {
            font-size: 2.5rem;
            margin-bottom: 0.5rem;
            background: linear-gradient(135deg, #667eea, #764ba2);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            font-weight: 800;
        }

        .profile-title {
            color: #667eea;
            font-size: 1.2rem;
            margin-bottom: 0.5rem;
            font-weight: 600;
        }

        .profile-bio {
            color: #b0b9c1;
            margin-bottom: 1rem;
            line-height: 1.6;
        }

        .profile-stats {
            display: flex;
            gap: 2rem;
            margin-top: 1rem;
        }

        .stat {
            display: flex;
            flex-direction: column;
        }

        .stat-number {
            font-size: 1.8rem;
            font-weight: 700;
            color: #667eea;
        }

        .stat-label {
            color: #b0b9c1;
            font-size: 0.9rem;
            margin-top: 0.25rem;
        }

        .action-buttons {
            display: flex;
            gap: 1rem;
            margin-top: 1.5rem;
            flex-wrap: wrap;
        }

        .btn {
            padding: 0.75rem 1.5rem;
            border: none;
            border-radius: 10px;
            cursor: pointer;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            font-size: 0.95rem;
        }

        .btn-primary {
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
            box-shadow: 0 10px 20px rgba(102, 126, 234, 0.3);
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 15px 30px rgba(102, 126, 234, 0.5);
        }

        .btn-secondary {
            background: rgba(102, 126, 234, 0.1);
            color: #667eea;
            border: 2px solid #667eea;
        }

        .btn-secondary:hover {
            background: rgba(102, 126, 234, 0.2);
            transform: translateY(-2px);
        }

        /* Section */
        .section {
            margin-bottom: 3rem;
            animation: slideIn 0.7s ease backwards;
        }

        .section:nth-child(2) {
            animation-delay: 0.1s;
        }

        .section:nth-child(3) {
            animation-delay: 0.2s;
        }

        .section h2 {
            font-size: 2rem;
            margin-bottom: 1.5rem;
            background: linear-gradient(135deg, #667eea, #764ba2);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            font-weight: 800;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        /* Projects Grid */
        .projects-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 2rem;
        }

        .project-card {
            background: linear-gradient(135deg, rgba(102, 126, 234, 0.1) 0%, rgba(118, 75, 162, 0.1) 100%);
            border: 2px solid #667eea;
            border-radius: 15px;
            padding: 2rem;
            transition: all 0.3s ease;
            cursor: pointer;
            position: relative;
            overflow: hidden;
        }

        .project-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.1), transparent);
            transition: left 0.5s ease;
        }

        .project-card:hover::before {
            left: 100%;
        }

        .project-card:hover {
            transform: translateY(-10px);
            border-color: #764ba2;
            box-shadow: 0 20px 40px rgba(102, 126, 234, 0.3);
        }

        .project-card h3 {
            font-size: 1.4rem;
            margin-bottom: 0.5rem;
            color: #fff;
            font-weight: 700;
        }

        .project-card p {
            color: #b0b9c1;
            margin-bottom: 1rem;
            line-height: 1.6;
        }

        .technologies {
            background: rgba(102, 126, 234, 0.1);
            border-left: 3px solid #667eea;
            padding: 0.75rem;
            border-radius: 8px;
            margin-bottom: 1rem;
            font-size: 0.9rem;
            color: #b0b9c1;
        }

        .project-actions {
            display: flex;
            gap: 0.5rem;
            justify-content: space-between;
        }

        .project-actions a,
        .project-actions button {
            flex: 1;
            padding: 0.6rem;
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            text-decoration: none;
            font-weight: 600;
            font-size: 0.85rem;
            transition: all 0.3s ease;
            text-align: center;
        }

        .project-actions a:hover,
        .project-actions button:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(102, 126, 234, 0.3);
        }

        .delete-btn {
            background: linear-gradient(135deg, #ff6b6b, #ee5a52) !important;
        }

        .delete-btn:hover {
            box-shadow: 0 10px 20px rgba(255, 107, 107, 0.3) !important;
        }

        .empty-state {
            text-align: center;
            padding: 3rem 2rem;
            background: linear-gradient(135deg, rgba(102, 126, 234, 0.05) 0%, rgba(118, 75, 162, 0.05) 100%);
            border: 2px dashed #667eea;
            border-radius: 15px;
            animation: slideIn 0.5s ease;
        }

        .empty-state i {
            font-size: 3rem;
            color: #667eea;
            display: block;
            margin-bottom: 1rem;
            opacity: 0.6;
        }

        .empty-state p {
            color: #b0b9c1;
            font-size: 1.1rem;
            margin-bottom: 1.5rem;
        }

        .empty-state a {
            display: inline-block;
            padding: 0.75rem 1.5rem;
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
            text-decoration: none;
            border-radius: 10px;
            font-weight: 600;
            transition: all 0.3s ease;
        }

        .empty-state a:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 30px rgba(102, 126, 234, 0.3);
        }

        /* Responsive */
        @media (max-width: 768px) {
            .profile-header {
                flex-direction: column;
                text-align: center;
                padding: 2rem;
            }

            .profile-info h1 {
                font-size: 1.8rem;
            }

            .profile-stats {
                justify-content: center;
                gap: 1.5rem;
            }

            .projects-grid {
                grid-template-columns: 1fr;
            }

            .nav-menu {
                gap: 1rem;
                font-size: 0.9rem;
            }

            .action-buttons {
                flex-direction: column;
            }

            .btn {
                width: 100%;
                justify-content: center;
            }
        }
    </style>
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar">
        <div class="nav-container">
            <a href="/" class="logo"><i class="fas fa-rocket"></i> SP</a>
            <ul class="nav-menu">
                <li><a href="/" class="nav-link">Home</a></li>
                <li><a href="/projects" class="nav-link">Projects</a></li>
                <li><a href="/dashboard" class="nav-link active">Dashboard</a></li>
            </ul>
            <div class="user-info">
                <img src="{{user.avatar}}" alt="Profile" class="user-avatar">
                <span>{{user.name}}</span>
                <a href="/logout" class="logout-btn"><i class="fas fa-sign-out-alt"></i> Logout</a>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <div class="container">
        <!-- Profile Header -->
        <div class="profile-header">
            <img src="{{user.avatar}}" alt="{{user.name}}" class="profile-avatar">
            <div class="profile-info">
                <h1>Welcome, {{user.name}}! 👋</h1>
                <p class="profile-title">{{user.title}}</p>
                {{#if user.bio}}
                <p class="profile-bio">{{user.bio}}</p>
                {{/if}}
                {{#if user.location}}
                <p class="profile-bio"><i class="fas fa-map-marker-alt"></i> {{user.location}}</p>
                {{/if}}
                <div class="profile-stats">
                    <div class="stat">
                        <div class="stat-number">{{projects.length}}</div>
                        <div class="stat-label">Total Projects</div>
                    </div>
                    <div class="stat">
                        <div class="stat-number">{{#if user.skills}}{{user.skills}}{{else}}5+{{/if}}</div>
                        <div class="stat-label">Skills</div>
                    </div>
                </div>
                <div class="action-buttons">
                    <a href="/profile/{{user._id}}" class="btn btn-primary">
                        <i class="fas fa-edit"></i> Edit Profile
                    </a>
                    <a href="/customize" class="btn btn-primary">
                        <i class="fas fa-palette"></i> Customize Portfolio
                    </a>
                    <a href="/add-project" class="btn btn-secondary">
                        <i class="fas fa-plus"></i> Add Project
                    </a>
                </div>
            </div>
        </div>

        <!-- Projects Section -->
        <div class="section">
            <h2><i class="fas fa-briefcase"></i> Your Projects</h2>
            
            {{#if projects}}
            <div class="projects-grid">
                {{#each projects}}
                <div class="project-card" style="animation-delay: {{@index}}00ms;">
                    <h3>{{this.title}}</h3>
                    <p>{{this.description}}</p>
                    {{#if this.technologies}}
                    <div class="technologies">
                        <i class="fas fa-code"></i> {{this.technologies}}
                    </div>
                    {{/if}}
                    <div class="project-actions">
                        {{#if this.link}}
                        <a href="{{this.link}}" target="_blank"><i class="fas fa-external-link-alt"></i> Live</a>
                        {{/if}}
                        {{#if this.github}}
                        <a href="{{this.github}}" target="_blank"><i class="fab fa-github"></i> Code</a>
                        {{/if}}
                        <a href="/project/{{this._id}}"><i class="fas fa-info-circle"></i> Details</a>
                        <form method="POST" action="/delete-project/{{this._id}}" style="display:contents;">
                            <button type="submit" class="delete-btn" onclick="return confirm('Are you sure you want to delete this project?');"><i class="fas fa-trash"></i> Delete</button>
                        </form>
                    </div>
                </div>
                {{/each}}
            </div>
            {{else}}
            <div class="empty-state">
                <i class="fas fa-folder-open"></i>
                <p>No projects yet. Start building your portfolio!</p>
                <a href="/add-project"><i class="fas fa-plus"></i> Create First Project</a>
            </div>
            {{/if}}
        </div>

        <!-- Quick Stats Section -->
        <div class="section">
            <h2><i class="fas fa-chart-line"></i> Portfolio Stats</h2>
            <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 1.5rem;">
                <div style="background: linear-gradient(135deg, rgba(102, 126, 234, 0.1) 0%, rgba(118, 75, 162, 0.1) 100%); border: 2px solid #667eea; border-radius: 15px; padding: 2rem; text-align: center;">
                    <div style="font-size: 2.5rem; color: #667eea; font-weight: bold; margin-bottom: 0.5rem;">{{projects.length}}</div>
                    <div style="color: #b0b9c1;">Projects Created</div>
                </div>
                <div style="background: linear-gradient(135deg, rgba(102, 126, 234, 0.1) 0%, rgba(118, 75, 162, 0.1) 100%); border: 2px solid #667eea; border-radius: 15px; padding: 2rem; text-align: center;">
                    <div style="font-size: 2.5rem; color: #667eea; font-weight: bold; margin-bottom: 0.5rem;">{{#if user.skills}}{{user.skills}}{{else}}5+{{/if}}</div>
                    <div style="color: #b0b9c1;">Skills Listed</div>
                </div>
                <div style="background: linear-gradient(135deg, rgba(102, 126, 234, 0.1) 0%, rgba(118, 75, 162, 0.1) 100%); border: 2px solid #667eea; border-radius: 15px; padding: 2rem; text-align: center;">
                    <div style="font-size: 2.5rem; color: #667eea; font-weight: bold; margin-bottom: 0.5rem;"><i class="fas fa-check-circle"></i></div>
                    <div style="color: #b0b9c1;">Profile Complete</div>
                </div>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer style="background: rgba(15, 20, 25, 0.95); border-top: 2px solid #667eea; padding: 2rem; text-align: center; color: #b0b9c1; margin-top: 3rem;">
        <p>&copy; 2024 {{user.name}}'s Portfolio. All rights reserved.</p>
    </footer>
</body>
</html>
