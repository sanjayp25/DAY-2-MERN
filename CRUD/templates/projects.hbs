<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Projects - John Doe</title>
    <link rel="stylesheet" href="/css/home.css">
</head>
<body>
    <nav class="navbar">
        <div class="nav-container">
            <a href="/" class="logo">JD</a>
            <ul class="nav-menu">
                <li><a href="/" class="nav-link">Home</a></li>
                <li><a href="/about" class="nav-link">About</a></li>
                <li><a href="/projects" class="nav-link">Projects</a></li>
                <li><a href="/contact" class="nav-link">Contact</a></li>
                <li><a href="/add-project" class="nav-link">Add Project</a></li>
            </ul>
        </div>
    </nav>

    <section class="content-section">
        <div class="container">
            <h1>My Projects</h1>
            <div class="projects-grid">
                {{#if projects}}
                    {{#each projects}}
                    <div class="project-card">
                        <h3>{{this.title}}</h3>
                        <p>{{this.description}}</p>
                        <div class="technologies">
                            <strong>Technologies:</strong> {{this.technologies}}
                        </div>
                        <div class="project-links">
                            {{#if this.link}}<a href="{{this.link}}" target="_blank" class="link-btn">View Project</a>{{/if}}
                            {{#if this.github}}<a href="{{this.github}}" target="_blank" class="link-btn">GitHub</a>{{/if}}
                            <a href="/project/{{this._id}}" class="link-btn">Details</a>
                            <form action="/delete-project/{{this._id}}" method="POST" style="display:inline;">
                                <button type="submit" class="link-btn delete-btn" onclick="return confirm('Are you sure?')">Delete</button>
                            </form>
                        </div>
                    </div>
                    {{/each}}
                {{else}}
                    <p style="color: #b0b9c1; text-align: center; grid-column: 1/-1;">No projects yet. <a href="/add-project" style="color: #0066ff;">Add your first project!</a></p>
                {{/if}}
            </div>
        </div>
    </section>

    <footer class="footer">
        <p>&copy; 2024 John Doe. All rights reserved.</p>
    </footer>

    <style>
        .content-section {
            min-height: calc(100vh - 200px);
            padding: 80px 20px;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        .content-section h1 {
            font-size: 2.5rem;
            margin-bottom: 2rem;
            background: linear-gradient(135deg, #0066ff, #ff0066);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .projects-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
            gap: 2rem;
        }

        .project-card {
            background: #232a3e;
            padding: 2rem;
            border-radius: 12px;
            border: 1px solid #2d3748;
            transition: all 0.3s ease;
        }

        .project-card:hover {
            border-color: #0066ff;
            transform: translateY(-5px);
            box-shadow: 0 10px 30px rgba(0, 102, 255, 0.2);
        }

        .project-card h3 {
            color: #00d4ff;
            margin-bottom: 0.5rem;
        }

        .project-card p {
            color: #b0b9c1;
            margin-bottom: 1rem;
            line-height: 1.6;
        }

        .technologies {
            color: #b0b9c1;
            font-size: 0.9rem;
            margin-bottom: 1rem;
        }

        .project-links {
            display: flex;
            gap: 0.5rem;
            flex-wrap: wrap;
        }

        .link-btn {
            background: #0066ff;
            color: white;
            padding: 6px 12px;
            border-radius: 5px;
            text-decoration: none;
            font-size: 0.85rem;
            transition: all 0.3s ease;
            border: none;
            cursor: pointer;
        }

        .link-btn:hover {
            background: #00d4ff;
        }

        .delete-btn {
            background: #ff0066;
        }

        .delete-btn:hover {
            background: #cc0052;
        }
    </style>
</body>
</html>
